build

# Gerados pelo idf.py — o que se versiona e o sdkconfig.defaults
sdkconfig
sdkconfig.old

# Credenciais locais (nao versionar)
.env

managed_components

# Artefatos de compilacao dos testes de host
test/host/run_tests
test/host/run_tests.dSYM/

# Configuracao local do Claude Code (skills, permissoes)
.claude/